# Gamma
alfa <- 2
beta <- 3
# Poisson
lambda <- 5
normal <- rnorm(5.5,mean = 5,sd =sigma/sqrt(n))
gamma <- rgamma(n,alfa,rate=beta)
poss <- rpois(n,lambda)
# Realizar el muestreo de las distribuciones.
for(i in 1:n) {
}
# Código para gráficos
graficar_histograma <- function(medias_muestrales, titulo) {
hist(medias_muestrales, probability = TRUE, main = titulo, xlab = "Media muestral")
curve(dnorm(x, mean = mean(medias_muestrales), sd = sd(medias_muestrales)), col = "red", lwd = 2, add = TRUE)
}
graficar_histograma(normal,'Normal')
# Definición de variables o estructuras necesarias para el muestreo.
n <- 1000
# Normal
sigma <- sqrt(5)
mu <-5
# Gamma
alfa <- 2
beta <- 3
# Poisson
lambda <- 5
normal <- rnorm(5.5,mean = 5,sd =sigma/sqrt(n))
gamma <- rgamma(n,alfa,rate=beta)
poss <- rpois(n,lambda)
# Realizar el muestreo de las distribuciones.
for(i in 1:n) {
}
# Código para gráficos
graficar_histograma <- function(medias_muestrales, titulo) {
hist(medias_muestrales, probability = TRUE, main = titulo, xlab = "Media muestral")
curve(dnorm(x, mean = mean(medias_muestrales), sd = sd(medias_muestrales)), col = "red", lwd = 2, add = TRUE)
}
graficar_histograma(normal,'Normal')
graficar_histograma(gamma,'Gamma')
graficar_histograma(poss,'Poisson')
# Definición de variables o estructuras necesarias para el muestreo.
n <- 1000
# Normal
sigma <- sqrt(5)
mu <-5
# Gamma
alfa <- 2
beta <- 3
# Poisson
lambda <- 5
normal <- rnorm(1,mean = 5,sd =sigma/sqrt(n))
gamma <- rgamma(n,alfa,rate=beta)
poss <- rpois(n,lambda)
# Realizar el muestreo de las distribuciones.
for(i in 1:n) {
}
# Código para gráficos
graficar_histograma <- function(medias_muestrales, titulo) {
hist(medias_muestrales, probability = TRUE, main = titulo, xlab = "Media muestral")
curve(dnorm(x, mean = mean(medias_muestrales), sd = sd(medias_muestrales)), col = "red", lwd = 2, add = TRUE)
}
graficar_histograma(normal,'Normal')
graficar_histograma(gamma,'Gamma')
graficar_histograma(poss,'Poisson')
# Definición de variables o estructuras necesarias para el muestreo.
n <- 1000
# Normal
sigma <- sqrt(5)
mu <-5
# Gamma
alfa <- 2
beta <- 3
# Poisson
lambda <- 5
normal <- rnorm(n,mean = 5,sd =sigma/sqrt(n))
gamma <- rgamma(n,alfa,rate=beta)
poss <- rpois(n,lambda)
# Realizar el muestreo de las distribuciones.
for(i in 1:n) {
}
# Código para gráficos
graficar_histograma <- function(medias_muestrales, titulo) {
hist(medias_muestrales, probability = TRUE, main = titulo, xlab = "Media muestral")
curve(dnorm(x, mean = mean(medias_muestrales), sd = sd(medias_muestrales)), col = "red", lwd = 2, add = TRUE)
}
graficar_histograma(normal,'Normal')
graficar_histograma(gamma,'Gamma')
graficar_histograma(poss,'Poisson')
# Definición de variables o estructuras necesarias para el muestreo.
n <- 5000
# Normal
sigma <- sqrt(5)
mu <-5
# Gamma
alfa <- 2
beta <- 3
# Poisson
lambda <- 5
normal <- rnorm(n,mean = 5,sd =sigma/sqrt(n))
gamma <- rgamma(n,alfa,rate=beta)
poss <- rpois(n,lambda)
# Realizar el muestreo de las distribuciones.
for(i in 1:n) {
}
# Código para gráficos
graficar_histograma <- function(medias_muestrales, titulo) {
hist(medias_muestrales, probability = TRUE, main = titulo, xlab = "Media muestral")
curve(dnorm(x, mean = mean(medias_muestrales), sd = sd(medias_muestrales)), col = "red", lwd = 2, add = TRUE)
}
graficar_histograma(normal,'Normal')
graficar_histograma(gamma,'Gamma')
graficar_histograma(poss,'Poisson')
# Definición de variables o estructuras necesarias para el muestreo.
n <- 50000
# Normal
sigma <- sqrt(5)
mu <-5
# Gamma
alfa <- 2
beta <- 3
# Poisson
lambda <- 5
normal <- rnorm(n,mean = 5,sd =sigma/sqrt(n))
gamma <- rgamma(n,alfa,rate=beta)
poss <- rpois(n,lambda)
# Realizar el muestreo de las distribuciones.
for(i in 1:n) {
}
# Código para gráficos
graficar_histograma <- function(medias_muestrales, titulo) {
hist(medias_muestrales, probability = TRUE, main = titulo, xlab = "Media muestral")
curve(dnorm(x, mean = mean(medias_muestrales), sd = sd(medias_muestrales)), col = "red", lwd = 2, add = TRUE)
}
graficar_histograma(normal,'Normal')
graficar_histograma(gamma,'Gamma')
graficar_histograma(poss,'Poisson')
# Definición de variables o estructuras necesarias para el muestreo.
n <- 50000
# Normal
sigma <- sqrt(5)
mu <-5
# Gamma
alfa <- 2.5
beta <- 3
# Poisson
lambda <- 5
normal <- rnorm(n,mean = 5,sd =sigma/sqrt(n))
gamma <- rgamma(n,alfa,rate=beta)
poss <- rpois(n,lambda)
# Realizar el muestreo de las distribuciones.
for(i in 1:n) {
}
# Código para gráficos
graficar_histograma <- function(medias_muestrales, titulo) {
hist(medias_muestrales, probability = TRUE, main = titulo, xlab = "Media muestral")
curve(dnorm(x, mean = mean(medias_muestrales), sd = sd(medias_muestrales)), col = "red", lwd = 2, add = TRUE)
}
graficar_histograma(normal,'Normal')
graficar_histograma(gamma,'Gamma')
graficar_histograma(poss,'Poisson')
# Definición de variables o estructuras necesarias para el muestreo.
n <- 50000
# Normal
sigma <- sqrt(5)
mu <-5
# Gamma
alfa <- 2.5
beta <- 3
# Poisson
lambda <- 3
normal <- rnorm(n,mean = 5,sd =sigma/sqrt(n))
gamma <- rgamma(n,alfa,rate=beta)
poss <- rpois(n,lambda)
# Realizar el muestreo de las distribuciones.
for(i in 1:n) {
}
# Código para gráficos
graficar_histograma <- function(medias_muestrales, titulo) {
hist(medias_muestrales, probability = TRUE, main = titulo, xlab = "Media muestral")
curve(dnorm(x, mean = mean(medias_muestrales), sd = sd(medias_muestrales)), col = "red", lwd = 2, add = TRUE)
}
graficar_histograma(normal,'Normal')
graficar_histograma(gamma,'Gamma')
graficar_histograma(poss,'Poisson')
# Definición de variables o estructuras necesarias para el muestreo.
n <- 50000
# Normal
sigma <- sqrt(5)
mu <-5
# Gamma
alfa <- 2
beta <- 1.5
# Poisson
lambda <- 5
normal <- rnorm(n,mean = 5,sd =sigma/sqrt(n))
gamma <- rgamma(n,alfa,rate=beta)
poss <- rpois(n,lambda)
# Realizar el muestreo de las distribuciones.
for(i in 1:n) {
}
# Código para gráficos
graficar_histograma <- function(medias_muestrales, titulo) {
hist(medias_muestrales, probability = TRUE, main = titulo, xlab = "Media muestral")
curve(dnorm(x, mean = mean(medias_muestrales), sd = sd(medias_muestrales)), col = "red", lwd = 2, add = TRUE)
}
graficar_histograma(normal,'Normal')
graficar_histograma(gamma,'Gamma')
graficar_histograma(poss,'Poisson')
# Definición de variables o estructuras necesarias para el muestreo.
n <- 50000
# Normal
sigma <- sqrt(5)
mu <-5
# Gamma
alfa <- 2
beta <- 1.5
# Poisson
lambda <- 5
normal <- rnorm(n,mean = 5,sd =sigma/sqrt(n))
gamma <- rgamma(n,alfa,rate=beta)
poss <- rpois(n,lambda)
# Realizar el muestreo de las distribuciones.
for(i in 1:n) {
}
# Código para gráficos
graficar_histograma <- function(medias_muestrales, titulo) {
hist(medias_muestrales, probability = TRUE, main = titulo, xlab = "Media muestral")
curve(dnorm(x, mean = mean(medias_muestrales), sd = sd(medias_muestrales)), col = "red", lwd = 2, add = TRUE)
}
graficar_histograma(normal,'Normal')
graficar_histograma(gamma,'Gamma')
graficar_histograma(poss,'Poisson')
# Definición de variables o estructuras necesarias para el muestreo.
n <- 50000
# Normal
sigma <- sqrt(5)
mu <-5
# Gamma
alfa <- 2
beta <- 1.5
# Poisson
lambda <- 5
normal <- rnorm(n,mean = 5,sd =sigma/sqrt(n))
gamma <- rgamma(n,alfa,rate=beta)
poss <- rpois(n,lambda)
# Realizar el muestreo de las distribuciones.
for(i in 1:n) {
}
# Código para gráficos
graficar_histograma <- function(medias_muestrales, titulo) {
hist(medias_muestrales, probability = TRUE, main = titulo, xlab = "Media muestral")
curve(dnorm(x, mean = mean(medias_muestrales), sd = sd(medias_muestrales)), col = "red", lwd = 2, add = TRUE)
}
graficar_histograma(normal,'Normal')
graficar_histograma(gamma,'Gamma')
graficar_histograma(poss,'Poisson')
# Definición de variables o estructuras necesarias para el muestreo.
n <- 10
# Normal
sigma <- sqrt(5)
mu <-5
# Gamma
alfa <- 2
beta <- 1.5
# Poisson
lambda <- 5
normal <- rnorm(n,mean = 5,sd =sigma/sqrt(n))
normal_promedio <- numeric(n)
gamma <- rgamma(n,alfa,rate=beta)
gamma_promedio <-numeric(n)
poss <- rpois(n,lambda)
# Realizar el muestreo de las distribuciones.
for(i in 1:n) {
normal_promedio[i] <- mean(normal)
}
# Código para gráficos
graficar_histograma <- function(medias_muestrales, titulo) {
hist(medias_muestrales, probability = TRUE, main = titulo, xlab = "Media muestral")
curve(dnorm(x, mean = mean(medias_muestrales), sd = sd(medias_muestrales)), col = "red", lwd = 2, add = TRUE)
}
graficar_histograma(normal,'Normal')
graficar_histograma(normal_promedio,'Normal')
graficar_histograma(gamma,'Gamma')
graficar_histograma(poss,'Poisson')
# Definición de variables o estructuras necesarias para el muestreo.
n <- 1000
# Normal
sigma <- sqrt(5)
mu <-5
# Gamma
alfa <- 2
beta <- 1.5
# Poisson
lambda <- 5
normal <- rnorm(n,mean = 5,sd =sigma/sqrt(n))
normal_promedio <- numeric(n)
gamma <- rgamma(n,alfa,rate=beta)
gamma_promedio <-numeric(n)
poss <- rpois(n,lambda)
# Realizar el muestreo de las distribuciones.
for(i in 1:n) {
normal_promedio[i] <- mean(normal)
}
# Código para gráficos
graficar_histograma <- function(medias_muestrales, titulo) {
hist(medias_muestrales, probability = TRUE, main = titulo, xlab = "Media muestral")
curve(dnorm(x, mean = mean(medias_muestrales), sd = sd(medias_muestrales)), col = "red", lwd = 2, add = TRUE)
}
graficar_histograma(normal,'Normal')
graficar_histograma(normal_promedio,'Normal')
graficar_histograma(gamma,'Gamma')
graficar_histograma(poss,'Poisson')
# Definición de variables o estructuras necesarias para el muestreo.
n <- 1000
tamaño <- 100
# Normal
sigma <- sqrt(5)
mu <-5
# Gamma
alfa <- 2
beta <- 1.5
# Poisson
lambda <- 5
normal_promedio <- numeric(n)
gamma <- rgamma(n,alfa,rate=beta)
gamma_promedio <-numeric(tamaño)
poss <- rpois(n,lambda)
# Realizar el muestreo de las distribuciones.
for(i in 1:n) {
normal <- rnorm(tamaño,mean = 5,sd =sigma/sqrt(n))
normal_promedio[i] <- mean(normal)
}
# Código para gráficos
graficar_histograma <- function(medias_muestrales, titulo) {
hist(medias_muestrales, probability = TRUE, main = titulo, xlab = "Media muestral")
curve(dnorm(x, mean = mean(medias_muestrales), sd = sd(medias_muestrales)), col = "red", lwd = 2, add = TRUE)
}
graficar_histograma(normal,'Normal')
graficar_histograma(normal_promedio,'Normal')
graficar_histograma(gamma,'Gamma')
graficar_histograma(poss,'Poisson')
# Definición de variables o estructuras necesarias para el muestreo.
n <- 1000
tamaño <- 100
# Normal
sigma <- sqrt(5)
mu <-5
# Gamma
alfa <- 2
beta <- 1.5
# Poisson
lambda <- 5
normal_promedio <- numeric(n)
gamma_promedio <-numeric(n)
poss_promedio <-numeric(n)
# Realizar el muestreo de las distribuciones.
for(i in 1:n) {
normal <- rnorm(tamaño,mean = 5,sd =sigma/sqrt(n))
normal_promedio[i] <- mean(normal)
gamma <- rgamma(tamaño,alfa,rate=beta)
gamma_promedio[i] <- mean(gamma)
poss <- rpois(tamaño,lambda)
poss_promedio[i] <- mean(poss)
}
# Código para gráficos
graficar_histograma <- function(medias_muestrales, titulo) {
hist(medias_muestrales, probability = TRUE, main = titulo, xlab = "Media muestral")
curve(dnorm(x, mean = mean(medias_muestrales), sd = sd(medias_muestrales)), col = "red", lwd = 2, add = TRUE)
}
graficar_histograma(normal,'1')
graficar_histograma(normal_promedio,'Normal')
graficar_histograma(gamma,'2')
graficar_histograma(gamma_promedio,'Gamma')
graficar_histograma(poss,'3')
graficar_histograma(poss_promedio,'Poisson')
# Definición de variables o estructuras necesarias para el muestreo.
n <- 1000
tamaño <- 10
# Normal
sigma <- sqrt(5)
mu <-5
# Gamma
alfa <- 2
beta <- 1.5
# Poisson
lambda <- 5
normal_promedio <- numeric(n)
gamma_promedio <-numeric(n)
poss_promedio <-numeric(n)
# Realizar el muestreo de las distribuciones.
for(i in 1:n) {
normal <- rnorm(tamaño,mean = 5,sd =sigma/sqrt(n))
normal_promedio[i] <- mean(normal)
gamma <- rgamma(tamaño,alfa,rate=beta)
gamma_promedio[i] <- mean(gamma)
poss <- rpois(tamaño,lambda)
poss_promedio[i] <- mean(poss)
}
# Código para gráficos
graficar_histograma <- function(medias_muestrales, titulo) {
hist(medias_muestrales, probability = TRUE, main = titulo, xlab = "Media muestral")
curve(dnorm(x, mean = mean(medias_muestrales), sd = sd(medias_muestrales)), col = "red", lwd = 2, add = TRUE)
}
graficar_histograma(normal,'Normal 1')
graficar_histograma(normal_promedio,'Normal')
graficar_histograma(gamma,'Gamma 2')
graficar_histograma(gamma_promedio,'Gamma')
graficar_histograma(poss,'Poisson 3')
graficar_histograma(poss_promedio,'Poisson')
# Definición de variables o estructuras necesarias para el muestreo.
n <- 1000
tamaño <- 100
# Normal
sigma <- sqrt(5)
mu <-5
# Gamma
alfa <- 2
beta <- 1.5
# Poisson
lambda <- 5
normal_promedio <- numeric(n)
gamma_promedio <-numeric(n)
poss_promedio <-numeric(n)
# Realizar el muestreo de las distribuciones.
for(i in 1:n) {
normal <- rnorm(tamaño,mean = 5,sd =sigma/sqrt(n))
normal_promedio[i] <- mean(normal)
gamma <- rgamma(tamaño,alfa,rate=beta)
gamma_promedio[i] <- mean(gamma)
poss <- rpois(tamaño,lambda)
poss_promedio[i] <- mean(poss)
}
# Código para gráficos
graficar_histograma <- function(medias_muestrales, titulo) {
hist(medias_muestrales, probability = TRUE, main = titulo, xlab = "Media muestral")
curve(dnorm(x, mean = mean(medias_muestrales), sd = sd(medias_muestrales)), col = "red", lwd = 2, add = TRUE)
}
graficar_histograma(normal,'Normal 1')
graficar_histograma(normal_promedio,'Normal')
graficar_histograma(gamma,'Gamma 2')
graficar_histograma(gamma_promedio,'Gamma')
graficar_histograma(poss,'Poisson 3')
graficar_histograma(poss_promedio,'Poisson')
# Definición de variables o estructuras necesarias para el muestreo.
n <- 1000
tamaño <- 1000
# Normal
sigma <- sqrt(5)
mu <-5
# Gamma
alfa <- 2
beta <- 1.5
# Poisson
lambda <- 5
normal_promedio <- numeric(n)
gamma_promedio <-numeric(n)
poss_promedio <-numeric(n)
# Realizar el muestreo de las distribuciones.
for(i in 1:n) {
normal <- rnorm(tamaño,mean = 5,sd =sigma/sqrt(n))
normal_promedio[i] <- mean(normal)
gamma <- rgamma(tamaño,alfa,rate=beta)
gamma_promedio[i] <- mean(gamma)
poss <- rpois(tamaño,lambda)
poss_promedio[i] <- mean(poss)
}
# Código para gráficos
graficar_histograma <- function(medias_muestrales, titulo) {
hist(medias_muestrales, probability = TRUE, main = titulo, xlab = "Media muestral")
curve(dnorm(x, mean = mean(medias_muestrales), sd = sd(medias_muestrales)), col = "red", lwd = 2, add = TRUE)
}
graficar_histograma(normal,'Normal 1')
graficar_histograma(normal_promedio,'Normal')
graficar_histograma(gamma,'Gamma 2')
graficar_histograma(gamma_promedio,'Gamma')
graficar_histograma(poss,'Poisson 3')
graficar_histograma(poss_promedio,'Poisson')
# Definición de variables o estructuras necesarias para el muestreo.
n <- 1000
tamaño <- 5000
# Normal
sigma <- sqrt(5)
mu <-5
# Gamma
alfa <- 2
beta <- 1.5
# Poisson
lambda <- 5
normal_promedio <- numeric(n)
gamma_promedio <-numeric(n)
poss_promedio <-numeric(n)
# Realizar el muestreo de las distribuciones.
for(i in 1:n) {
normal <- rnorm(tamaño,mean = 5,sd =sigma/sqrt(n))
normal_promedio[i] <- mean(normal)
gamma <- rgamma(tamaño,alfa,rate=beta)
gamma_promedio[i] <- mean(gamma)
poss <- rpois(tamaño,lambda)
poss_promedio[i] <- mean(poss)
}
# Código para gráficos
graficar_histograma <- function(medias_muestrales, titulo) {
hist(medias_muestrales, probability = TRUE, main = titulo, xlab = "Media muestral")
curve(dnorm(x, mean = mean(medias_muestrales), sd = sd(medias_muestrales)), col = "red", lwd = 2, add = TRUE)
}
graficar_histograma(normal,'Normal 1')
graficar_histograma(normal_promedio,'Normal')
graficar_histograma(gamma,'Gamma 2')
graficar_histograma(gamma_promedio,'Gamma')
graficar_histograma(poss,'Poisson 3')
graficar_histograma(poss_promedio,'Poisson')
